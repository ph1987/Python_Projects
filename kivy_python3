
# Instalando python3-kivy no Linux Mint e Umbuto

# Instalar Kivy:

apt install python3-kivy -y

# Dependências com o SDL2:
# Exemplo do Ubunto, Mint...:

apt install -y \
    python3-pip \
    build-essential \
    git \
    python3-dev \
    ffmpeg \
    libsdl2-dev \
    libsdl2-image-dev \
    libsdl2-mixer-dev \
    libsdl2-ttf-dev \
    libportmidi-dev \
    libswscale-dev \
    libavformat-dev \
    libavcodec-dev \
    zlib1g-dev
    
# Instale o gstreamer para áudio, vídeo(opcional):
apt install -y \
    libgstreamer1.0 \
    gstreamer1.0-plugins-base \
    gstreamer1.0-plugins-good
    
# Dependências com o PyGame legado:
# Instalar pacotes de sistema necessários
    
apt install -y \
    python3-pip \
    build-essential \
    mercurial \
    git \
    python3-dev \
    ffmpeg \
    libsdl-image1.2-dev \
    libsdl-mixer1.2-dev \
    libsdl-ttf2.0-dev \
    libsmpeg-dev \
    libsdl1.2-dev \
    libportmidi-dev \
    libswscale-dev \
    libavformat-dev \
    libavcodec-dev \
    zlib1g-dev
    
# Certifique-se de que o Virtualenv e Setuptools estejam atualizados:

pip3 install --upgrade virtualenv setuptools

# Em seguida, crie um virtualenv chamado "kivylab":

# (isto irá usar o interpretador em /usr/bin/python3.76):
virtualenv --no-site-packages -p /usr/bin/python3.6 kivylab

# Digite o virtualenv:

. kivylab/bin/activate

# Use a versão correta do Cython aqui:

pip3 install Cython==0.28.2

# Instale a versão estável do Kivy no virtualenv:

pip3 install kivy

#
